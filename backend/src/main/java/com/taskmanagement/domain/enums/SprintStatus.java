package com.taskmanagement.domain.enums;

/**
 * Enum representing the status of a sprint
 * Tracks the lifecycle of a sprint in the Scrum process
 */
public enum SprintStatus {
    PLANNED,
    ACTIVE,
    COMPLETED
}
