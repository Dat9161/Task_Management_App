package com.taskmanagement.domain.enums;

/**
 * Enum representing different types of notifications
 * Used to categorize and handle notifications appropriately
 */
public enum NotificationType {
    TASK_ASSIGNED,
    STATUS_CHANGED,
    SPRINT_STARTED,
    SPRINT_COMPLETED,
    DEADLINE_APPROACHING
}
